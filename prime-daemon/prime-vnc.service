[Unit]
Description=Prime wayvnc screen mirroring server
After=graphical-session.target
PartOf=graphical-session.target

[Service]
ExecStart=/usr/bin/wayvnc -C %h/.config/wayvnc/config
Restart=on-failure
RestartSec=3

[Install]
WantedBy=graphical-session.target
